juju:
  model-defaults:
    test-mode: "true"
    automatically-retry-hooks: "false"
  channel: 3.6/stable
  extra-bootstrap-args: --config idle-connection-timeout=180s
providers:
  lxd:
    enable: true
    bootstrap: true
host:
  packages:
    - python3-pip
  snaps:
    charmcraft:
    jq:
    yq:
